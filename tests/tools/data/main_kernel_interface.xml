<?xml version="1.0"?>
<!-- The kernel's argument description, which soda-opt emits alongside the test
     vectors. bambu has no option that reads it, so it must not become a
     --generate-tb argument. -->
<module>
  <function id="main_kernel">
    <arg id="a" interface_type="array" size="4"/>
  </function>
</module>
